jwt:
  # Clave secreta (debe tener al menos 256 bits / 32 caracteres)
  secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970"
  expiration: 86400000 # 24 horas en milisegundos
app:
  reset-token-expiration-minutes: 10 
  base-url: localhost
  protocol: http://
  port: 8080
  gateway-url: http://gateway-ms
spring:
  security:
    csrf:
      enabled: false
  mail:
      host: smtp.gmail.com
      port: 587
      username: gang.emelec@gmail.com
      password: ozuagiytouaywscf 
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/edu_llm
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      initialization-fail-timeout: -1
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.springframework.jdbc.core: DEBUG
    org.springframework.jdbc.core.StatementCreatorUtils: TRACE
    org.springframework.transaction: DEBUG
    com.zaxxer.hikari: DEBUG
    config: DEBUG
    controller: DEBUG
    services: DEBUG
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
  metrics:
    export:
      otlp:
        enabled: true
        url: http://localhost:4318/v1/metrics
      prometheus:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics