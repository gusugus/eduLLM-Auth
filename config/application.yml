jwt:
  secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970"
  expiration: 86400000
app:
  reset-token-expiration-minutes: 10
  base-url: localhost
  protocol: http:///
  port: 8081
  gateway-url: http://localhost:8085
spring:
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  security:
    csrf:
      enabled: false
  mail:
      host: smtp.gmail.com
      port: 587
      username: gang.emelec@gmail.com
      password: abc 
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/edullm
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    services: DEBUG
    controller: DEBUG
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
  metrics:
    export:
      otlp:
        enabled: true
        url: http://localhost:4318/v1/metrics
      prometheus:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics